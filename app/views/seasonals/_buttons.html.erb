<% Seasonal.seasonal_types.each do |type, _| %>
  <% if student.valid_seasonal(type) %>
    <%= render 'seasonals/valid_button', seasonal_type: type, student: student %>
  <% else %>
    <%= render 'seasonals/missing_button', seasonal_type: type, student: student %>
  <% end %>
<% end %>
