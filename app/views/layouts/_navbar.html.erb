<nav class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Team Manager", root_url, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="collapse">
      <% if current_user %>
        <ul class="nav navbar-nav">
          <li class="<%= active_class(students_path) %> dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Students<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "List", students_path %></li>
              <li><%= link_to "Dashboard", dashboard_students_path %></li>
            </ul>
          </li>
          <li class="<%= active_class(teams_path) %>"><%= link_to "Teams", teams_path %></li>
        </ul>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Admin sign in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
