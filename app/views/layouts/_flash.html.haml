- flash.each do |type, message|
  - unless type == 'timedout'
    %div{ "class" => "alert #{bootstrap_class_for_flash(type)} text-center alert-warning"}
      = message
      %button{ "class" => "close", "data-dismiss" => "alert"} ×
