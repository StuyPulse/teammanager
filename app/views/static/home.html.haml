%br
%div.card.text-center.bg-primary.text-white
  %div.card-header
    %h1 Welcome to Team Manager!
%br

- if current_user
  - if current_user.student
    = render partial: "student_homepage"
  - if current_user.is_admin
    = render partial: "admin_homepage"
- else
  %div.text-center
    %h3 Use your StuyPulse Google Account to enter the website!
