%h1 Events

.table-responsive
  %table.table.table-hover.table-condensed
    %thead
      %tr
        %th Name
        %th Description
        %th Default hours

    %tbody
      - @events.each do |event|
        %tr
          %td= event.name
          %td= event.description
          %td= event.default_hours
